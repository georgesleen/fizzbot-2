services:
  fizzbot:
    image: georgesleen/fizzbot:latest
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DISCORD_TOKEN: ${DISCORD_TOKEN}
    volumes:
      - ./llm/runs:/workspace/llm/runs
      - ./llm/train_data:/workspace/llm/train_data
