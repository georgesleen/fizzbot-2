# LLM

The fizzbot LLM is developed in this directory.

## Docker

For ease of training on any system, a dockerfile is provided, with necessary packages for GPU acceleration.

## Data

Put your data in ```data/``` to be automatically ignored from version control
